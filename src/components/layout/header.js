import React, { useState, useContext } from "react";
import styled from "styled-components";
import { respondTo } from "../../utils/stylesHelper";

const Container = styled.header``;

const Header = () => {
  return <hi>Header is here</hi>;
};

export default Header;
